<%= form_with(model: user_attribute) do |form| %>
  <% if user_attribute.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_attribute.errors.count, "error") %> prohibited this user_attribute from being saved:</h2>

      <ul>
        <% user_attribute.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.select :user_id,  options_for_select(User.all.map{|s|[s.email, s.id]}) %>
  </div>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :value, style: "display: block" %>
    <%= form.text_field :values %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
