<%= form_with(model: operation_attribute) do |form| %>
  <% if operation_attribute.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(operation_attribute.errors.count, "error") %> prohibited this operation_attribute from being saved:</h2>

      <ul>
        <% operation_attribute.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :values, style: "display: block" %>
    <%= form.text_field :values %>
  </div>

  <div>
    <%= form.label :operation_id, style: "display: block" %>
    <%= form.select :operation_id, options_for_select(Operation.all.map{|s|[s.name, s.id]}) %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
